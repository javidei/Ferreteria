# Reglas del repositorio

## Versionado

- Versiones de tres bloques (`x.x.x`).
- Cada entrega incrementa `version.json` y el pie de las páginas.
- La misma entrega añade una sección en `CHANGELOG.md`.
